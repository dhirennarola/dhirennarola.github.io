---
interface Props {
  /** Light pages start the header in its white-glass state */
  light?: boolean;
}

const { light = false } = Astro.props;

const links = [
  { href: '/services', label: 'Services' },
  { href: '/services/ai-odoo', label: 'AI + Odoo', match: '/services/ai-odoo' },
  { href: '/industries', label: 'Industries' },
  { href: '/work', label: 'Work' },
  { href: '/insights', label: 'Insights' },
  { href: '/about', label: 'About' },
];

const current = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (l: (typeof links)[number]) => {
  if (l.match) return current.startsWith(l.match);
  // "Services" must not light up while on the AI + Odoo flagship page
  if (l.href === '/services') return current.startsWith('/services') && !current.startsWith('/services/ai-odoo');
  return current.startsWith(l.href);
};
---

<header class:list={['hd', { 'hd--light': light }]} id="hd">
  <div class="hd-inner">
    <a href="/" class="brand" aria-label="Dhiren Narola — home">
      dhiren narola<span class="brand-dot">.</span>
    </a>

    <nav class="hd-links" id="hdLinks" aria-label="Main navigation">
      {links.map((l) => (
        <a href={l.href} class:list={[{ active: isActive(l) }]}>{l.label}</a>
      ))}
      <a href="/contact" class="btn btn--primary hd-cta">
        Book a call <span class="arr" aria-hidden="true">→</span>
      </a>
    </nav>

    <button
      class="hd-toggle"
      id="hdToggle"
      aria-expanded="false"
      aria-controls="hdLinks"
      aria-label="Open menu"
    >
      <span></span><span></span><span></span>
    </button>
  </div>
  <div class="hd-progress" id="hdProgress" aria-hidden="true"></div>
</header>

<style>
  .hd {
    position: fixed;
    inset-inline: 0;
    top: 0;
    z-index: 100;
    height: var(--nav-h);
    transition:
      background var(--t-med) ease,
      box-shadow var(--t-med) ease,
      height var(--t-med) var(--ease-out);
  }

  .hd-inner {
    max-width: var(--c-lg);
    margin-inline: auto;
    padding-inline: var(--gutter);
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--s-5);
  }

  @media (min-width: 1680px) {
    .hd-inner {
      max-width: var(--c-xl);
    }
  }

  /* Wordmark — lowercase, Manrope ExtraBold, gradient dot */
  .brand {
    font-family: var(--font-brand);
    font-weight: 800;
    font-size: 1.18rem;
    letter-spacing: -0.03em;
    color: var(--td);
    white-space: nowrap;
    line-height: 1;
    transition: color var(--t-med) ease;
  }

  .brand-dot {
    background: var(--grad);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .hd-links {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 2.2vw, 1.9rem);
  }

  .hd-links a:not(.hd-cta) {
    font-size: var(--text-sm);
    font-weight: 530;
    color: var(--td-soft);
    padding: 0.4rem 0;
    position: relative;
    transition: color var(--t-fast) ease;
  }

  .hd-links a:not(.hd-cta):hover {
    color: #fff;
  }

  .hd-links a.active::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: -2px;
    height: 2px;
    border-radius: 2px;
    background: var(--grad);
  }

  .hd-cta {
    height: 40px;
    padding-inline: 1.1rem;
  }

  /* scrolled → compact white glass */
  .hd.scrolled {
    height: 58px;
    background: rgba(255, 255, 255, 0.94);
    -webkit-backdrop-filter: blur(14px);
    backdrop-filter: blur(14px);
    box-shadow:
      0 1px 0 var(--line),
      0 8px 24px -12px rgba(14, 27, 46, 0.14);
  }

  .hd.scrolled .brand {
    color: var(--ink-strong);
  }

  .hd.scrolled .hd-links a:not(.hd-cta) {
    color: var(--ink-soft);
  }

  .hd.scrolled .hd-links a:not(.hd-cta):hover {
    color: var(--accent-strong);
  }

  /* light pages start in white-glass */
  .hd--light {
    background: rgba(255, 255, 255, 0.94);
    -webkit-backdrop-filter: blur(14px);
    backdrop-filter: blur(14px);
    box-shadow: 0 1px 0 var(--line);
  }

  .hd--light .brand {
    color: var(--ink-strong);
  }

  .hd--light .hd-links a:not(.hd-cta) {
    color: var(--ink-soft);
  }

  .hd--light .hd-links a:not(.hd-cta):hover {
    color: var(--accent-strong);
  }

  /* scroll progress hairline */
  .hd-progress {
    position: absolute;
    left: 0;
    bottom: 0;
    height: 2px;
    width: 0;
    background: var(--grad);
  }

  /* mobile toggle */
  .hd-toggle {
    display: none;
    width: 44px;
    height: 44px;
    background: none;
    border: 0;
    cursor: pointer;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 5px;
    color: var(--td);
    transition: color var(--t-med) ease;
  }

  .hd.scrolled .hd-toggle,
  .hd--light .hd-toggle {
    color: var(--ink-strong);
  }

  .hd-toggle span {
    width: 22px;
    height: 2px;
    border-radius: 2px;
    background: currentColor;
    transition:
      transform var(--t-med) ease,
      opacity var(--t-med) ease;
  }

  @media (max-width: 900px) {
    .hd-toggle {
      display: flex;
    }

    .hd-links {
      position: fixed;
      inset: var(--nav-h) 0 0 0;
      flex-direction: column;
      align-items: stretch;
      background: var(--navy-950);
      padding: var(--s-6) var(--gutter);
      gap: 0;
      transform: translateX(100%);
      transition:
        transform 450ms var(--ease-out),
        visibility 0s linear 450ms;
      overflow-y: auto;
      visibility: hidden;
    }

    .hd-links.open {
      transform: none;
      visibility: visible;
      transition: transform 450ms var(--ease-out);
    }

    .hd-links a:not(.hd-cta) {
      color: var(--td) !important;
      font-size: 1.15rem;
      padding: 1rem 0;
      border-bottom: 1px solid var(--navy-line);
    }

    .hd-links a.active::after {
      display: none;
    }

    .hd-links a.active {
      color: var(--accent-ondark) !important;
    }

    .hd-cta {
      margin-top: var(--s-5);
      height: 52px;
    }

    .hd-toggle[aria-expanded='true'] span:nth-child(1) {
      transform: translateY(7px) rotate(45deg);
    }

    .hd-toggle[aria-expanded='true'] span:nth-child(2) {
      opacity: 0;
    }

    .hd-toggle[aria-expanded='true'] span:nth-child(3) {
      transform: translateY(-7px) rotate(-45deg);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hd,
    .hd-links,
    .hd-toggle span {
      transition: none;
    }
  }
</style>

<style is:global>
  body.nav-open {
    overflow: hidden;
  }
</style>

<script>
  const hd = document.getElementById('hd')!;
  const progress = document.getElementById('hdProgress')!;
  const toggle = document.getElementById('hdToggle')!;
  const links = document.getElementById('hdLinks')!;
  const isLight = hd.classList.contains('hd--light');

  let ticking = false;
  const update = () => {
    ticking = false;
    hd.classList.toggle('scrolled', window.scrollY > (isLight ? 10 : 40));
    const max = document.documentElement.scrollHeight - window.innerHeight;
    progress.style.width = (max > 0 ? (window.scrollY / max) * 100 : 0) + '%';
  };
  window.addEventListener(
    'scroll',
    () => {
      if (!ticking) {
        ticking = true;
        requestAnimationFrame(update);
      }
    },
    { passive: true }
  );
  update();

  toggle.addEventListener('click', () => {
    const open = links.classList.toggle('open');
    toggle.setAttribute('aria-expanded', String(open));
    toggle.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
    document.body.classList.toggle('nav-open', open);
  });

  links.addEventListener('click', (e) => {
    if ((e.target as HTMLElement).closest('a')) {
      links.classList.remove('open');
      toggle.setAttribute('aria-expanded', 'false');
      document.body.classList.remove('nav-open');
    }
  });
</script>
